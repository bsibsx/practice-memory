# Practice Memory frontend

React and TypeScript interface for Practice Memory. The frontend is developed with Vite and proxies `/api` requests to the Spring Boot backend on port `8000`.

```bash
npm ci
npm run dev
```

Available checks:

```bash
npm run build
npm run lint
```

See the [root README](../README.md) for complete project setup and scope.
